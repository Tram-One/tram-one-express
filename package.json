{
	"name": "tram-one-express",
	"version": "19.0.1",
	"description": "Generator to build Tram-One applications quickly",
	"bin": "./generator.js",
	"files": [
		"template",
		"generator.js"
	],
	"main": "generator.js",
	"keywords": [
		"tram-one",
		"generator"
	],
	"scripts": {
		"generate-example": "npm run example-generate",
		"example-generate": "rm -rf tram-one-example && node generator.js tram-one-example",
		"example-build": "cd ./tram-one-example && npm run build",
		"example-test": "cd ./tram-one-example && npm test",
		"example-test-ci": "cd ./tram-one-example && npx jest",
		"ci-run": "node tests/circleci-run.js",
		"deploy-example": "gh-pages -d tram-one-example -b tram-one-example",
		"example-deploy": "cd ./tram-one-example && npm ci && npm run deploy",
		"postpublish:build": "rm -rf tram-one-example && node generator.js tram-one-example --no-install --no-commit --url tram-one-express",
		"postpublish": "npm run postpublish:build && npm run deploy-example && npm run example-deploy"
	},
	"author": "Jesse Jurman",
	"repository": "https://github.com/jrjurman/tram-one-express",
	"license": "MIT",
	"dependencies": {
		"commander": "^9.2.0",
		"fs-extra": "^10.1.0",
		"simple-git": "^3.7.1"
	},
	"devDependencies": {
		"gh-pages": "^4.0.0",
		"require-yml": "^2.0.0"
	}
}
